node_modules/
dist/
matches/
data/exports/
# Saved teams are personal/local — created at runtime, not shipped with the repo.
data/my-teams/
# Sticky UI preferences sidecar (storage/prefs.ts) — per-machine.
data/prefs.json
# Fetched sprite PNG cache (tui spriteCache.ts) — refetched on demand.
data/sprites/
# Training-data artifacts (derived from the replay corpus — regenerable).
data/training/
data/bring-labels.json
# Bring-search playout ground truth + derived bring sheets/matrices (regenerable
# via bring-search / bring-matrix / bring-guide — the 4v4 Nash corpus + cheat-sheets).
data/bring-truth*.json
data/bring-matrix-*.json
data/matrices/
data/cell-cache.*.json
data/bring-sheet-nash.*.md
data/bring-guide.*.md
data/replays-train/
# Creator-intel: fetched captions + derived threat teams (regenerable from a URL).
data/captions/
data/threats/
# Live on-the-fly Pikalytics sidecar — per-session fetches; the curated warm-up
# file (pikalytics.<fmt>.json) is tracked, this live cache is not.
data/pikalytics.*.live.json
# Sim-grounded matchup prep sheets (regenerable via mb-prep-sheets).
data/prep/
*.tsbuildinfo

# Server runtime sqlite data (native dev) — Docker uses a named volume
.data/
.data-test/
packages/server/.data/
packages/server/.data-test/

.env
.env.local
*.log
# …but the replay-validation corpus fixtures ARE tracked (offline CI tests).
!packages/core/tests/replays/*.log

# Vision capture/experiment scratch — screenshots, sprite crops, OCR model.
# (data/sprite-refs.json, the bootstrapped reference TABLE, lives under data/ and IS tracked.)
packages/vision/fixtures/
eng.traineddata

# editor / OS cruft
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code per-project local settings (machine-specific)
.claude/settings.local.json
